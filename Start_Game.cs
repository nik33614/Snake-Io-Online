using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Start_Game : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    //начало игры. 
    //отправляется запросм на сервер с созданием новой таблицы
    //заносится в таюлицу имя первого участника автоматически
    //через другой скрипт люди подсоединяются и их номера добавляются 12345
    //скрипт по запросу кидает номер каждому игроку.
    //номера на каждом устройстве равны



    // Update is called once per frame
    void Update()
    {
        
    }
}
